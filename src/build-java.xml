<?xml version="1.0" encoding="UTF-8"?>

<project name="ASPRO 2" default="about" basedir=".">

  <description>
  	Aspro ant build file
  </description>

  <!-- first load local config file -->
  <property file="build.properties"/>

  <property name="root" location=".."/>

  <property name="libs" location="${root}/lib"/>
  <property name="sources" location="${root}/src"/>


  <!-- custom macro to enhance XJC -->
  <macrodef name="xjc">
    <attribute name="destdir"/>
    <attribute name="schema"/>
    <attribute name="package"/>

    <sequential>
      <java jar="${libs}/jaxb/jaxb-xjc.jar">
        <arg value="-classpath"/>
        <arg value="${libs}/jaxb/simple-regenerator-1.0.jar"/>

        <arg value="-extension"/>
        <!-- preserve code extension -->
        <arg value="-simple-preserve"/>

        <arg value="-d"/>
        <arg value="@{destdir}"/>

        <arg value="-p"/>
        <arg value="@{package}"/>

        <arg value="@{schema}"/>
      </java>
      <delete dir="@{destdir}">
        <include name="**/*.java.backup"/>
      </delete>
      </sequential>
  </macrodef>


  <!-- targets -->

  <!-- target about : -->
  <target name="about" depends="init" description="show information for public targets">
    <echo>available ant targets : </echo>
    <echo>-------------------------------------------------------------------------------</echo>
    <echo>  - genXsdCode         : generates the java code from xml schema (xsd)</echo>
    <echo>-------------------------------------------------------------------------------</echo>
  </target>


  <!-- target init : timestamp -->
  <target name="init">
    <tstamp>
      <format property="build.date" pattern="dd/MM/yyyy HH:mm:ss" />
    </tstamp>

    <echo>-------------------------------------------------------------------------------</echo>
    <echo>  ${vendor}  ::  ${title} - ${version} </echo>
    <echo>-------------------------------------------------------------------------------</echo>
    <echo> Build File </echo>
    <echo>-------------------------------------------------------------------------------</echo>
    <echo> contacts     : ${build.by}  </echo>
    <echo> build date   : ${build.date} </echo>
    <echo></echo>
    <echo>-------------------------------------------------------------------------------</echo>

  </target>




  <target name="genXsdCode" depends="init">

    <echo>generating java code for AsproOIModel.xsd</echo>
    <mkdir dir="${sources}/fr/jmmc/aspro/model/oi" />

    <xjc destdir="${sources}" package="fr.jmmc.aspro.model.oi"
         schema="${sources}/fr/jmmc/aspro/model/AsproOIModel.xsd" />

    <echo>generating java code for AsproDataModel.xsd</echo>

    <mkdir dir="${sources}/fr/jmmc/aspro/model/oifits" />
    <xjc destdir="${sources}" package="fr.jmmc.aspro.model.oifits"
         schema="${sources}/fr/jmmc/aspro/model/AsproDataModel.xsd" />

  </target>


</project>
